language: python
sudo: false

services:
  - docker

cache:
  directories:
    - $TRAVIS_BUILD_DIR/wheelhouse

before_install:
  - docker version
  - ./wzl setup

install:
  - ./wzl build --all
  - pip install codecov

script:
  - ./wzl test

after_success:
  - codecov
